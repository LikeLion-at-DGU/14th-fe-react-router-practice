// 실습 1: 버튼 컴포넌트 만들기
import styled from 'styled-components';

import React from 'react';

const StyledButton = styled.button``;

// TODO 3. label, bgColor를 props로 받아 사용하세요.
//    - label: 버튼 안에 보여줄 텍스트
//    - bgColor: 버튼 배경색

const Button = () => {
  return (
  <></>
  );
};

export default Button;
